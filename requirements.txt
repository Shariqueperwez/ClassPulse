streamlit>=1.32.0
opencv-python-headless>=4.8.0
mediapipe>=0.10.0
numpy>=1.24.0
pandas>=2.0.0
plotly>=5.18.0
reportlab>=4.0.0
ultralytics>=8.0.0
Pillow>=10.0.0

# Needed for live browser-webcam support (streamlit-webrtc)
streamlit-webrtc>=0.47.0
av>=10.0.0

# Optional — only needed for the AI-written session summary feature.
# The app runs fully without it; that feature just stays hidden.
groq>=0.9.0
